name: ultralytics_yolo11n_pose_fp16

frontend:
  checkpoint: /path/to/yolo11n-pose.onnx
  shape:
    images: [1, 3, 640, 640]
  type: onnx
  dtype: fp32


backend:
  type: tvm_vacc
  dtype: fp16
  compile:
    cluster_mode: 0

workspace:
  path: ./deploy_weights/

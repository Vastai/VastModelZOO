name: elic_gs_fp16

frontend:
  shape:
    y_hat: [1,320,32,32]
  checkpoint: /path/to/gs.onnx
  type: onnx
  dtype: fp32

backend:
  type: tvm_vacc
  dtype: fp16
  compile:
    data_transport_mode: 1
    mem_inplace: true
    opt_level: 2

workspace:
  path: ./deploy_weights

